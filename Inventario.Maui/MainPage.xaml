<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Inventario.Maui.MainPage"
             xmlns:vm ="clr-namespace:Inventario.Maui.ViewModels"
             x:DataType="vm:InventarioViewModel">

    <ScrollView>
        <!--<VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            --><!--<Label Text="{Binding Numero}" FontSize="Body"  Background="Gray"/>
            <Label Text="{Binding Nombre}" FontSize="Body"  Background="Gray"/>--><!--

            <Button
                x:Name="CounterBtn"
                Text="Click me"
                
                --><!--Command="{Binding TestCommand}"--><!--
                HorizontalOptions="Center" />

        </VerticalStackLayout>-->
    </ScrollView>

</ContentPage>
